const stringUpperCase = (string) => string.toUpperCase();

const stringFirstLetter = (string) => string[0];

const stringConcat = (string, string2) => string.concat(string2);

module.exports = { stringUpperCase,  stringFirstLetter, stringConcat};

